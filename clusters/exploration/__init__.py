"""Cluster exploration — query Kubernetes resources via stored credentials.

This package is intentionally separate from cluster CRUD so it can grow
(namespaces, workloads, logs, etc.) without coupling to forms and admin UI.
"""
